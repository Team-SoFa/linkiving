import { tv } from 'tailwind-variants';

export const style = tv({
  base: [
    'pointer-events-none absolute z-50 rounded bg-black px-2 py-1 text-xs leading-tight font-medium tracking-wide whitespace-nowrap text-white shadow-lg',
  ],
});
